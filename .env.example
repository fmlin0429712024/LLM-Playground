# Copy this file to .env and add only the configuration required by your provider.
# Never commit .env.
APP_ENV=development
APP_ALLOWED_ORIGINS=http://localhost:3000

# Example provider settings (leave blank until an adapter is implemented).
# GOOGLE_CLOUD_PROJECT=
# VERTEX_AI_LOCATION=us-central1
